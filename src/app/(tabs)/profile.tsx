import { View, Text } from "react-native";

export default function ProfileScreen() {
  return (
    <View>
      <Text>Profile - This screen is only used for the tab and will not have content</Text>
    </View>
  );
}