import { View, Text } from "react-native";

export default function SearchScreen() {
  return (
    <View>
      <Text>Search - This screen is only used for the tab and will not have content</Text>
    </View>
  );
}